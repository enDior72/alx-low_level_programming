/*****************************************************************************/
#include "main.h"
/**
 * _abs - returns the absolute value of a number
 *
 * Return: absolute value of input number
 * @n: the input number
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}

