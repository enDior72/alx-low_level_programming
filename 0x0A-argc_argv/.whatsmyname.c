#include <stdio.h>

/**
* main - print program name follow by new line
* @argc: size_of @argv
* @argv: pointer to an array of argument of the user
*
* Return: 0 (onSucess)
*/

int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}
